pragma solidity >=0.4.4 <0.7.0;

contract Enteros{
    //variables enteras sin signo
    uint mi_primer_entero;
    uint mi_primer_entero_inicializado = 3;
    uint cota = 5000;

    //variables enteras sin signo con un número wespecifico de bits
    uint8 numero = 182;
    uint64 entero = 7000;
    uint16 entero1;
    uint256 entero2;

    //variables enteras con signo
    int entero_negativo = -23;

    //variables enteras incializadas con cierto número de bits
    int8 ocho = -128;
}